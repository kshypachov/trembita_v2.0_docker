{{- with .Values.trembita_config.trembita_monitor_pod}}
apiVersion: v1
kind: Service
metadata:
  name: {{.name}}-service-2082
spec:
  type: ClusterIP
  ports:
    - port: 2082
      targetPort: 2082 {{/*Port for access to monitor REST API*/}}
  selector:
    app: {{.name}}
{{- end}}