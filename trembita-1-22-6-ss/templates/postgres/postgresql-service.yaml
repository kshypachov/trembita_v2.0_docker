apiVersion: v1
kind: Service
metadata:
  annotations:
{{/*    "helm.sh/hook": pre-install*/}}
  name: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: {{ .Values.postgresql.port }}
  selector:
    app: {{.name}}