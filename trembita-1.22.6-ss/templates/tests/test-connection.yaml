apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trembita-1.22.6-ss.fullname" . }}-test-connection"
  labels:
    {{- include "trembita-1.22.6-ss.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trembita-1.22.6-ss.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
